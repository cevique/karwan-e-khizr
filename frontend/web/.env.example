# Backend API URL (FastAPI)
VITE_API_URL=http://localhost:8000/api/v1

# Use mock data when backend is unavailable (true/false)
# Set to false only when backend transit endpoints are implemented
VITE_USE_MOCK_DATA=true
